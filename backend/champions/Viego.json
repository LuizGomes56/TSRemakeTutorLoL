{
  "Viego": {
    "Q": {
      "type": "physical",
      "area": true,
      "min": [
        "(15 + (0.7 * currentAD)) * (1 + critChance) * physicalMod",
        "(30 + (0.7 * currentAD)) * (1 + critChance) * physicalMod",
        "(45 + (0.7 * currentAD)) * (1 + critChance) * physicalMod",
        "(60 + (0.7 * currentAD)) * (1 + critChance) * physicalMod",
        "(75 + (0.7 * currentAD)) * (1 + critChance) * physicalMod"
      ],
      "max": [
        "(0.2 * currentAD) + (0.15 * currentAP)",
        "(0.2 * currentAD) + (0.15 * currentAP)",
        "(0.2 * currentAD) + (0.15 * currentAP)",
        "(0.2 * currentAD) + (0.15 * currentAP)",
        "(0.2 * currentAD) + (0.15 * currentAP)"
      ]
    },
    "W": {
      "type": "magic",
      "area": false,
      "min": [
        "(80 + currentAP) * magicMod",
        "(135 + currentAP) * magicMod",
        "(190 + currentAP) * magicMod",
        "(245 + currentAP) * magicMod",
        "(300 + currentAP) * magicMod"
      ],
      "max": []
    },
    "R": {
      "type": "physical",
      "area": true,
      "min": [
        "((1.2 * (1 + critChance) * currentAD)) * physicalMod",
        "((1.2 * (1 + critChance) * currentAD)) * physicalMod",
        "((1.2 * (1 + critChance) * currentAD)) * physicalMod"
      ],
      "max": [
        "((1.2 * (1 + critChance) * currentAD) + ((0.12 + 5 * bonusAD / 10000) * expectedHealth)) * physicalMod",
        "((1.2 * (1 + critChance) * currentAD) + ((0.16 + 5 * bonusAD / 10000) * expectedHealth)) * physicalMod",
        "((1.2 * (1 + critChance) * currentAD) + ((0.2 + 5 * bonusAD / 10000) * expectedHealth)) * physicalMod"
      ]
    }
  }
}